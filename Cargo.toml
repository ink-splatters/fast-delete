[package]
name = "fdel"
version = "0.3.0"
edition = "2024"
license = "MIT"

[dependencies]
anyhow = "1.0.104"
clap = { version = "4.6.6", features = ["derive"] }
libc = "0.2.189"
rayon = "1.12.0"

[profile.release]
lto = "fat"
codegen-units = 1
strip = true
panic = "abort"
